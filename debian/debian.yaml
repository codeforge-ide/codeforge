flutter_app: 
  command: codeforge
  arch: x64
  parent: /usr/local/lib
  nonInteractive: false

control:
  Package: codeforge
  Version: 1.0.0
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer:
  Description: AI focused code IDE!
  
#options:
#  exec_out_dir: debian/packages